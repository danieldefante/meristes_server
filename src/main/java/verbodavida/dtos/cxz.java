package verbodavida.dtos;

import org.modelmapper.ModelMapper;

import verbodavida.entities.Contato;
import verbodavida.entities.Endereco;
import verbodavida.entities.Pessoa;

public class cxz {

	
	/*public static void main(String[] args) {
		
		
		Pessoa entity = new Pessoa();
		
		entity.setNome("Jesus Cristo");
		Contato contato = new Contato();
		
		contato.setEmail("daniel@email.com");
		entity.setContato(contato );
		Endereco endereco = new Endereco();
		endereco.setBairro("ashdfuhuds");
		entity.setEndereco(endereco);
		
//		Pessoa nhaa = ConverterDTO.converterEntetyToDTO(entity);
	
		
		ModelMapper mapper = new ModelMapper();
		PessoaDTO dto = mapper.map(entity, PessoaDTO.class);
		
		
		System.out.println(dto.getContato().getEmail());
	}*/
}
